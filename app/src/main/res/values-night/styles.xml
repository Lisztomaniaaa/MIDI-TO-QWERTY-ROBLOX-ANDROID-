<?xml version="1.0" encoding="utf-8"?>
<!--
  Dark mode theme. Mirrors values/styles.xml because the base theme is already
  dark — we just need to keep the SAME parent (Material.NoActionBar) so the
  activity stays fullscreen and not Dialog-styled in night mode.
-->
<resources>
    <style name="AppTheme" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:colorPrimary">@color/accent_primary</item>
        <item name="android:colorAccent">@color/accent_secondary</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>
</resources>
